% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mrf_deseq2_exportTabs.R
\name{mrf_deseq2_exportTabs}
\alias{mrf_deseq2_exportTabs}
\title{Export Differential Expression Results to CSV}
\usage{
mrf_deseq2_exportTabs(x, title = "", logFC = 1, pvalue = 0.05)
}
\arguments{
\item{x}{A data frame containing gene expression data with columns for log2 fold change and p-values.}

\item{title}{A character string specifying the prefix of the output CSV file name. Default is an empty string.}

\item{logFC}{Numeric, the log2 fold change threshold for significance. Default is 1.}

\item{pvalue}{Numeric, the p-value threshold for significance. Default is 0.05.}
}
\value{
A CSV file with the processed differential expression results, including condition labels.
}
\description{
This function processes differential expression data to add condition labels and exports the results to a CSV file.
}
\examples{
\dontrun{
library(dplyr)
library(tidyr)
library(tibble)
library(readr)
# Example data
tbl <- data.frame(
  logFC = c(1.2, -1.5, 0.3, 2.0),
  pvalue = c(0.01, 0.04, 0.2, 0.03)
)
# Export results to CSV
mrf_deseq2_exportTabs(tbl, title = "results", logFC = 1, pvalue = 0.05)
}

}
